{
  "name": "site",
  "private": true,
  "version": "0.1.3",
  "description": "The Vaab site — landing page and browser playground",
  "scripts": {
    "build:web": "npm run build --prefix web",
    "serve": "npm run build:web && vaab serve main.vaab",
    "dev:web": "npm run dev --prefix web"
  }
}
